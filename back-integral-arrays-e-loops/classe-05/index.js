const original = [1, 4, 12, 21, 53, 88, 112];
const numerosPares = [];

for (let originals of original) {
  if (originals % 2 === 0) {
    numerosPares.push(originals);
  }
}
console.log(numerosPares);

const lista [999];

for (let numero of lista) {
  if (numero > 0) {
    lista.push(numero);
  }

  console.log(lista)
    ;
}